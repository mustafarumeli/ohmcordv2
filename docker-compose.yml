services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    environment:
      PORT: 8080
      HOST: 0.0.0.0
    ports:
      - "8080:8080"
    restart: unless-stopped
